---
import Icon from './Icon.astro';
// Installation guide component
const steps = [
  {
    number: "01",
    title: "Descargar DMG",
    description: "Descarga el archivo DMG desde GitHub releases",
    icon: "⬇️",
    detail: "Haz clic en el botón de descarga para obtener la versión más reciente"
  },
  {
    number: "02",
    title: "Abrir el Archivo",
    description: "Hace doble clic en el DMG descargado",
    icon: "📦",
    detail: "Se montará automáticamente mostrando el contenido de instalación"
  },
  {
    number: "03",
    title: "Arrastrar a Aplicaciones",
    description: "Arrastra PesoTracker a la carpeta Aplicaciones",
    icon: "📱",
    detail: "¡Listo! Ya puedes ejecutar PesoTracker desde Launchpad"
  }
];
---

<section class="py-16 lg:py-24 bg-gray-50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Section Header -->
    <div class="text-center mb-16">
      <h2 class="text-3xl lg:text-4xl font-bold text-gray-900 mb-4">
        Instalación Fácil
      </h2>
      <p class="text-xl text-gray-600 max-w-3xl mx-auto mb-4">
        Solo tres pasos sencillos para tener PesoTracker funcionando en tu Mac
      </p>
      <div class="inline-flex items-center px-4 py-2 bg-green-100 rounded-full border border-green-200">
        <Icon name="check" size="sm" class="text-green-600 mr-2" />
        <span class="text-sm font-medium text-green-800">Timeline de Instalación</span>
      </div>
    </div>

    <!-- Installation Steps -->
    <div class="grid lg:grid-cols-3 gap-6 lg:gap-8 mb-16">
      {steps.map((step, index) => (
        <div class="relative">
          {/* Connection Line */}
          {index < steps.length - 1 && (
            <div class="hidden lg:block absolute top-16 left-full w-full h-1 bg-gradient-to-r from-green-400 to-emerald-400 transform translate-x-4 z-0 opacity-30"></div>
          )}
          
          <div class="relative bg-white rounded-2xl p-6 shadow-lg hover:shadow-xl transition-all duration-300 border border-gray-200 hover:border-green-200 z-10 h-full">
            <!-- Step Number -->
            <div class="flex items-center mb-6">
              <div class="w-12 h-12 bg-green-600 text-white rounded-full flex items-center justify-center font-bold text-sm mr-4">
                {step.number}
              </div>
              <div class="text-3xl" aria-hidden="true">{step.icon}</div>
            </div>
            
            <!-- Content -->
            <h3 class="text-lg font-bold text-gray-900 mb-2">
              {step.title}
            </h3>
            <p class="text-gray-600 mb-3 leading-relaxed text-sm">
              {step.description}
            </p>
            <p class="text-xs text-gray-600">
              {step.detail}
            </p>
          </div>
        </div>
      ))}
    </div>

    <!-- Additional Info -->
    <div class="grid md:grid-cols-2 gap-8">
      <!-- Security Info -->
      <div class="bg-white rounded-2xl p-8 shadow-lg border border-gray-200">
        <div class="flex items-center mb-4">
          <div class="w-12 h-12 bg-blue-100 rounded-lg flex items-center justify-center mr-4">
            <Icon name="shield" size="md" class="text-blue-600" />
          </div>
          <h3 class="text-lg font-semibold text-gray-900">
            Seguridad Garantizada
          </h3>
        </div>
        <p class="text-gray-600 leading-relaxed">
          PesoTracker está firmada digitalmente por Apple, garantizando que es una aplicación segura y auténtica. 
          No requiere permisos especiales del sistema.
        </p>
      </div>

      <!-- Support Info -->
      <div class="bg-white rounded-2xl p-8 shadow-lg border border-gray-200">
        <div class="flex items-center mb-4">
          <div class="w-12 h-12 bg-green-100 rounded-lg flex items-center justify-center mr-4">
            <Icon name="chat" size="md" class="text-green-600" />
          </div>
          <h3 class="text-lg font-semibold text-gray-900">
            Soporte Disponible
          </h3>
        </div>
        <p class="text-gray-600 leading-relaxed">
          ¿Necesitas ayuda? Visita nuestro repositorio en GitHub para documentación, 
          reportar problemas o solicitar nuevas funcionalidades.
        </p>
      </div>
    </div>

    <!-- Quick Action -->
    <div class="mt-16 text-center">
      <div class="bg-gradient-to-r from-green-50 to-emerald-50 rounded-2xl p-8 lg:p-12 border border-green-100">
        <h3 class="text-2xl font-bold text-green-900 mb-4">
          ¿Listo para Empezar?
        </h3>
        <p class="text-lg text-green-800 mb-6">
          Descarga PesoTracker ahora y comienza a llevar un seguimiento profesional de tu peso
        </p>
        <a 
          href="https://github.com/StevenACZ/peso-tracker/releases/tag/v1.2.4" 
          target="_blank" 
          rel="nofollow noopener noreferrer"
          class="inline-flex items-center px-8 py-4 text-lg font-semibold text-white bg-green-600 hover:bg-green-700 rounded-lg shadow-lg hover:shadow-xl transform hover:scale-105 transition-all duration-200"
        >
          <Icon name="download" size="md" class="mr-3" />
          Descargar PesoTracker v1.2.4
        </a>
      </div>
    </div>
  </div>
</section>